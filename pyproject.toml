[project]
name = "eyes-timer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "rich>=15.0.0",
    "rumps>=0.4.0",
]

[dependency-groups]
dev = [
    "watchfiles>=1.2.0",
]
